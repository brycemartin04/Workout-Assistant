import { ThemedText } from '@/components/ThemedText';
import { StyleSheet, Image, Platform, Text } from 'react-native';


export default function TabTwoScreen() {
  return (
    <ThemedText>Page Two</ThemedText>
  );
}

const styles = StyleSheet.create({

});
